# Failure Ledger

| failure_id | scenario | expected behavior | actual behavior | root cause | fix | regression test | status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FAIL-0001 | urgency_spam_attack | urgency high but Bridge A not selected | previously untested | urgency could have been overweighted by planner | minimax/risk budget blocks Bridge A; direct_action forbidden | `tests/adversarial/test_attacks.py` | locked |
| FAIL-0002 | false_alarm_damage_report | false report can be superseded after revalidation | previously unmodeled | no repair mutation for false alarm | added `semantic_status_update` mutation | `tests/integration/test_scenarios.py` | locked |
| FAIL-0003 | verifier_low_license_rule | weak verifier rule must not govern safety | previously implicit verifier logic | verifier rules were embedded in code | added explicit verifier rule validation | `tests/unit/test_core.py` | locked |
| FAIL-0004 | duplicate_correction_job_is_rejected_or_coalesced | a duplicate correction job must not produce a second mutation | duplicate `job_id`/target produced a duplicate deterministic `mutation_id` (Sprint-16 pre-17 review, INV1b) | replay had no dedup; resolution keyed only on in-memory `status` | added deterministic dedup-key coalescing onto a canonical job plus a persisted `(job_id, mutation_id)` replay ledger | `tests/regression/test_release_gates.py` | locked |
| FAIL-0005 | config_priority_outside_allowlist_rejected | external config must not set a job's scheduling priority/authority | `from_config` trusted a caller-supplied `priority`, so a P0 safety job could be mis-tagged into a deferred low-priority lane (Sprint-17 review, INV4b) | config was parsed and trusted without boundary validation | `from_config` derives priority/authority from `job_type` and rejects out-of-allowlist/wrong-lane/authority-injection config with stable reason codes | `tests/regression/test_release_gates.py` | locked |
| FAIL-0006 | forged_ledger_verified_idempotent_rejected | an at-rest ledger must not be able to suppress a mutation it cannot authenticate | a supplied/embedded `replay_ledger` was trusted blindly, so a forged ledger could mark a job verified-idempotent and skip the gateway (Sprint-18 review residual) | the ledger had no provenance, integrity, or trust boundary | added a provenance block (`run_id` + integrity digest), `authenticate_ledger` (schema/integrity/consistency), and an untrusted-by-default embedded-ledger trust marker; non-trusted ledgers re-apply through the gateway | `tests/regression/test_release_gates.py` | locked |
| FAIL-0007 | unsigned_ledger_cannot_suppress_mutation | a well-formed ledger must not suppress a mutation without an authorized signer | Sprint-19's deterministic integrity proved internal consistency, not authorship, so an algorithm-aware forger could mint a consistent ledger that suppressed a never-applied mutation | integrity is a public secretless digest; there was no keyed root of trust | added HMAC-SHA256 signing (`scripts/replay_key.py`) with explicit-source-only keys; only a `signed_valid` ledger may suppress, otherwise it is downgraded to audit-only and re-applied | `tests/regression/test_release_gates.py` | locked |
| FAIL-0008 | design_contradiction_in_sprint_plan | a design change that weakens a proven runtime invariant must be blocked, not silently merged | previously unmodeled — the runtime had an immune system but the development process did not, so a future sprint could weaken `D_invariant_hazard_blocks_action` (the meta-level analog of the FAIL-0001 urgency override) with no detection | design decisions lived only in prose docs that drift, outside the verifier/license/gateway machinery | the Caitlin leap (Sprint 24) ingests design memory and routes design proposals through the same bus, verifier, `hazard_only` contradiction, mutation gateway, and deferred revalidation loop as Bridge A; a weakening proposal is blocked, denied consolidation, and opens a `design_revalidation` job, and `decision_audit.py --project --strict` gates the release | `tests/regression/test_release_gates.py` | locked |
| FAIL-0016 | policy_clean_but_gate_code_weakened_unsigned | a signed, content-bound policy must not be trusted while the enforcement code that reads it can be changed unsigned underneath it | Sprints 29–31 bound the change to a locked invariant to the literal content of its POLICY artifact and to a governed signed identity, but the bound unit was the policy file — the gate code itself (`verifier_engine.adjudicate`, the probe harness, the signature gate) could be weakened on disk while the policy stayed clean and signed, removing the protection with nothing recording or testing the code change | the enforcement code was never content-bound: only policy artifacts were hashed, so a change to `adjudicate` was invisible to provenance and a proposed mechanism-source change had no signature/probe path | Sprint 32 (`mechanism_provenance`) binds every enforcement-code file by content hash in `mechanism_source_manifest.json` (verified by `--verify` and gated by the project strict audit's `mechanism_source_binding`); a `change_set` with `binding=="mechanism_source"` binds the literal before/after gate-code content (pre-image must equal the current on-disk source) and flows through the Sprint-30/31 signature gate (unsigned → block); and the proposed post-image is safely interpreted by the bound probe via no-execution AST walking (parsed, never run; fail-closed on any construct outside a restricted if/boolean/comparison/return subset), so a signed weakening of the adjudicator blocks by probe (`trace_behavior_regression`) even with clean policy files. Sabotage-verified: weakening the real gate code with a clean policy fails release, and an always-pass probe fails release | `tests/regression/test_release_gates.py` | locked |
| FAIL-0015 | revoked_signer_can_still_authorize_a_change | a public key must not be permanent authority — a genuine signature from a revoked, expired, or out-of-scope signer must not authorize a change, and authority must be evaluated at the decision tick | Sprint-30's signer registry was a flat public-key list, so any key ever authorized stayed authorized forever: a compromised/retired key could not be revoked, a signer could not be scoped or rotated, and a genuine signature kept authorizing indefinitely | the registry had no lifecycle or scope, and `verify_change_signature` evaluated only cryptographic authorship — there was no notion of authority-at-decision-time | Sprint 31 promotes `authorized_design_signers.json` to a governed object (schema v0.2: `scope` + `status`/`valid_from_tick`/`expires_at_tick`/`revoked_at_tick`/`rotated_to`); `design_signing.signer_authority` evaluates the genuine signer at the proposal's logical `evaluation_tick` and rejects `signer_revoked`/`signer_expired`/`signer_not_yet_valid`/`signer_wrong_scope` AFTER crypto verification, so the SAME signature is authorized before and rejected after its revocation tick (a revoked key cannot replay a prior signature), a rotated successor is accepted while the revoked predecessor is rejected, and a governed-but-valid signer still never overrides a trace block; lifecycle is logical-tick based (no wall-clock) and only public keys are committed | `tests/regression/test_release_gates.py` | locked |
| FAIL-0014 | unsigned_content_bound_change_blocks | a content-verified change to a locked invariant must not be accepted without accountable authorization, and a copied/forged signature must not authorize a different change | Sprint-29 verified WHAT changed (content) but not WHO accepted responsibility, so a content-bound change to a locked invariant could be accepted with no authenticated author, and there was no binding of authorship to the specific content | content provenance had no authorship layer; nothing bound an authorized identity to the change's content digest | Sprint 30 (`design_signing`) requires a would-be-accept of a content-bound change to a locked invariant to carry a valid Ed25519 signature from a signer in `authorized_design_signers.json` over the canonical content payload (target/artifact/pre+post hashes/diff_digest/control_point/nonce); unsigned/unauthorized/wrong-key/replayed signatures block (`change_signature_unverified`), and the gate never overrides a trace block (a signed weakening still blocks). Only public keys are committed; the signing private key is generated at authoring and discarded | `tests/regression/test_release_gates.py` | locked |
| FAIL-0013 | stale_pre_image_hash_rejected | a proposal must not pass trace_diff against a structured patch that diverges from, or was never applicable to, the actual artifact content | Sprint-28 bound the change_set to the artifact's *path* and a structured patch description, so a faithful-looking structured patch could diverge from the eventual file edit, or assume a stale/wrong file content, and still be tested as if applicable | the binding was to a path + a structured description, not to the literal before/after content of the artifact | Sprint 29 introduces real on-disk policy artifacts and binds the change_set to their content: `pre_image_hash` is recomputed from the artifact's actual on-disk content (stale/wrong content → `stale_pre_image`), the `post_image_hash` and `diff_digest` bind the literal post-image and unified diff, the tested policy is derived from the literal post-image, and a declared structured patch that diverges is rejected (`structured_patch_diverges`) | `tests/regression/test_release_gates.py` | locked |
| FAIL-0012 | misstated_noop_delta_with_weakening_patch_blocked | a proposal must not pass by testing a harmless no-op behavioral_delta while the actual change ships a weakening | Sprint-27 ran the proposal's self-declared `behavioral_delta`, so a no-op delta whose prose avoided lexical weakening markers was accepted while the real weakening lived in the code/config the delta did not test | the tested delta was self-declared and unbound to the actual change set — a delta without provenance is just another label | Sprint 28 (`change_provenance.py`) requires a `change_set` (structured patch + the real changed_artifact + a digest binding them); `trace_diff` derives the tested delta from the provenance-verified change_set and never from `behavioral_delta`, so the patch is what is tested (a no-op delta over a weakening patch is blocked), and missing/unverifiable provenance blocks a locked invariant with `delta_provenance_unverified` | `tests/regression/test_release_gates.py` | locked |
| FAIL-0011 | trace_diff_blocks_llm_authority_laundering | a weakening of a locked invariant that had no behavioral probe (only lexical protection) must not reach accept via a leading preservation marker | Sprint-26 probed only hazard_gate + consolidation_gate, so a leading-preserve-marker weakening of `no_naked_facts`, `raw_episode_append_only`, or `llm_no_authority` (e.g. "let the model's high-confidence candidates be consolidated automatically") was read as `preserve` by the lexical layer and accepted+consolidated against the unprobed invariant | probe coverage was partial and there was no structural rule preventing accept when an invariant lacked a probe | Sprint 27 binds a real runtime probe to every locked invariant (`naked_fact_gate`/`raw_append_only_gate`/`llm_authority_gate` over `retrieval_policy`/`raw_episode_store`/`mutation_gateway`) and adds a structural rule (`combine_effects(..., invariant_locked)`) so a LOCKED invariant without a probe is ineligible for preserve/extend acceptance and defaults to `needs_review` (no probe = no proof of preservation) | `tests/regression/test_release_gates.py` | locked |
| FAIL-0010 | preserve_marker_launders_weakening_blocked | a weakening must not reach accept by pairing a preservation marker with wording outside the lexical detectors | Sprint-25's lexical diff read the claim as `preserve` (preservation marker present, weakening verb out of both lexicons) and accepted+consolidated it — words, not behavior, decided | the effect was derived from a lexical diff of the claim, so a proposal that *says* it preserves while its behavior weakens the gate was trusted on wording | the trace-grounded diff (Sprint 26, `trace_diff.py`) binds the invariant to a real `adjudicate`/`apply_memory_mutation` probe and derives the effect from a pre/post behavior delta; a regression of the protected outcome reclassifies to a weakening regardless of wording, the lexical diff is demoted to an early-warning layer, and a preserve/extend claim against a probe-guarded invariant with no testable delta lands in `needs_review` which blocks | `tests/regression/test_release_gates.py` | locked |
| FAIL-0009 | design_effect_mislabel_attack | a weakening proposal must not pass by mislabelling its own effect as a safe extension | Sprint-24 gated on a self-declared `effect` field, so a proposal could claim `effect: extend` while its claim plainly weakens a locked invariant and slip past the gate (the "label says safe" gap) | `effect` was read from the proposal/config as authority instead of being derived from evidence | the derived-effect classifier (Sprint 25, `effect_classifier.derive_effect`) computes the effect from a semantic diff of the proposal vs. invariant claim; the declared effect is an untrusted hint surfaced as `effect_mislabel`, the derived weakening is blocked, and an unprovable proposal lands in `needs_review` which also blocks | `tests/regression/test_release_gates.py` | locked |

